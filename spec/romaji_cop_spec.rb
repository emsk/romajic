require 'spec_helper'

describe RomajiCop do
  it 'has a version number' do
    expect(RomajiCop::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
